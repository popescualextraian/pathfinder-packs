# common

Shared agents and skills used across all roles in pathfinder-packs.

Place here anything that is not role-specific (e.g. document formatting helpers, diagram generation, repository navigation). Role areas should reference these instead of duplicating them.
